# Environment Configuration (contains sensitive credentials)
.env
.env.local
.env.*.local

# Database Configuration (legacy - now using .env)
includes/config/db.php.backup

# PHPMailer (should be managed via Composer)
PHPMailer-master/
PHPMailer.zip

# Composer dependencies
vendor/
composer.lock

# Environment files
.env
.env.local
.env.*.local

# IDE and Editor files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~
.project
.buildpath
.settings/

# OS-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Logs and databases
*.log
*.sqlite
*.db

# Database dumps and backups (but keep migration files!)
*.sql.gz
*.sql.zip
backup*.sql
dump*.sql
database/backups/

# Session files
sessions/
tmp/
temp/

# Upload directories (if any)
uploads/
upload/
images/uploads/
assets/

# Cache directories
cache/
*.cache

# Backup files
*.bak
*.backup
*.old
*.orig

# Package files
*.zip
*.tar.gz
*.rar
*.7z

# Exclude specific archives that were removed
PHPMailer*.zip

# Node modules (if using npm)
node_modules/
package-lock.json
yarn.lock

# Build files
dist/
build/
